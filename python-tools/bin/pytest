#!/bin/ash
set -e
[ $DEBUG ] && set -x

if [[ $1 ]];then
    test_path="/$1"
    shift
fi

if [ "$WRITE_COVERAGE" == "true" ];then
    options="--cov=/project/src${test_path}"
fi
py.test /project/src${test_path} $options $@
